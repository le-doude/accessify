package ${package}

import org.accessify.handlers.BasePOJOPropertiesHandler;

public class ${handlerClassName} extends BasePOJOPropertiesHandler<${enitityClassName}> {

    public ${handlerClassName}() {
        super(
            #foreach($propertyHandler in $propertyHandlers)
                new ${propertyHandler}(),
            #end
            null
        );
    }

    public Class<${enitityClassName}> handledType(){
        return ${enitityClassName}.class;
    }
}